## 1. 修复蹬墙跳 (Wall Jump)
- **防止转向冲突**: 修改 [PlayerController.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerController.cs) 中的转向逻辑，在 `WallJumpState` 期间锁定自动转向，防止玩家按住墙方向键时瞬间抵消跳跃冲力。
- **优化力施加**: 确保 [PlayerWallJumpState.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerWallJumpState.cs) 正确检测墙壁方向并施加推力。

## 2. 实现单向平台组合键下落 (One Way Platform Drop)
- **按键检测**: 在 [PlayerGroundedState.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerGroundedState.cs) 中添加 `S (下) + Jump` 的组合键检测。
- **调用逻辑**: 触发 [PlayerController.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerController.cs) 中已有的 `DisableCollision()` 协程，实现穿透效果。

## 3. 实现变量重力 (Variable Gravity)
- **新增配置**: 在 [PlayerController.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerController.cs) 中添加 `jumpGravityScale` (上升重力) 和 `fallGravityScale` (下降重力) 字段。
- **逻辑实现**: 在 [PlayerInAirState.cs](file:///d:/Desktop/Dead_refactor/angryBirds_Refactor/Assets/_Scripts/player/PlayerInAirState.cs) 的 `PhysicsUpdate` 中，根据当前垂直速度 `RB.velocity.y` 动态切换重力倍率。

## 4. 任务清单 (TODO)
- [ ] 在 `PlayerController` 中添加重力配置并锁定墙跳转向
- [ ] 在 `PlayerInAirState` 中实现上升/下降重力区分
- [ ] 在 `PlayerGroundedState` 中添加平台下落组合键
- [ ] 验证蹬墙跳力度与方向逻辑
- [ ] 更新 API 笔记

是否开始执行修复？